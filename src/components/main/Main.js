import React from "react";

const Main=props=>{
    return(
        <div 
            // style={{backgroundColor:'#000'}}
        >
            <h1 style={{color:'var(--body-color)'}}>Main</h1>
        </div>
    )
}

export default Main